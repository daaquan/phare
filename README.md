# API Skeleton

## Get Started

### Requirements

To run this application on your machine, you need at least:

* PHP >= 8.1
* Phalcon >= 5.0
* MySQL >= 8.0
* Nginx Web Server

### Installing Dependencies via Composer

```bash
cd app-folder
composer install
cp .env.example .env
```